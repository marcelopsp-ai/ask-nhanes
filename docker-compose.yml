version: '3.8'

services:
  ask-nhanes:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./data/chroma_db:/app/data/chroma_db
    restart: unless-stopped
